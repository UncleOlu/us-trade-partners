export function PeriodNote(): JSX.Element {
  return <p className="chart-note period-note">All years adds annual values in nominal USD; it is not an annual average or adjusted for inflation. A missing required year makes the period value absent. Years explicitly marked not applicable are excluded. EU values use each year's historical membership.</p>;
}
