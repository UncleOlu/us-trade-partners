/// <reference types="vite/client" />

// Injected by vite.config.ts `define`. snapshot_id and base path are build-time
// constants; never hard-code a snapshot id anywhere else in source.
declare const __SNAPSHOT_ID__: string;
declare const __BASE_PATH__: string;
