[project]
name = "us-trade-partners"
version = "0.1.0"
description = "Pipeline for US goods trade partner visualization (Agent A)"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.31",
    "pandas>=2.2",
    "pycountry>=24.6",
]
